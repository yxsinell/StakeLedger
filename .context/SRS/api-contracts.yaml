openapi: 3.0.3
info:
  title: StakeLedger API
  version: 1.0.0
  description: API para gestion de banks, apuestas, metas y recomendaciones.
servers:
  - url: http://localhost:3000
    description: Development
  - url: https://staging.example.com
    description: Staging
  - url: https://api.example.com
    description: Production

paths:
  /api/auth/register:
    post:
      summary: Register new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
            example:
              email: user@example.com
              password: StrongPass1
      responses:
        '201':
          description: Registration accepted. A confirmation email may be required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/auth/login:
    post:
      summary: Login user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login success. The web session is set through httpOnly cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/auth/logout:
    post:
      summary: Logout current user
      responses:
        '200':
          description: Session ended
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /api/auth/reset-password:
    post:
      summary: Request password reset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordRequest'
      responses:
        '200':
          description: Reset email sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/banks:
    get:
      summary: List banks
      security:
        - cookieAuth: []
      responses:
        '200':
          description: Bank list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      summary: Create bank
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BankCreateRequest'
      responses:
        '201':
          description: Bank created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/banks/{bankId}:
    get:
      summary: Get bank details
      security:
        - cookieAuth: []
      parameters:
        - $ref: '#/components/parameters/BankId'
      responses:
        '200':
          description: Bank details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/banks/{bankId}/transfer:
    post:
      summary: Transfer cash to another owned bank in the same currency
      security:
        - cookieAuth: []
      parameters:
        - $ref: '#/components/parameters/BankId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferCreateRequest'
      responses:
        '200':
          description: Existing idempotent transfer result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferResponse'
        '201':
          description: Transfer applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: Source or destination bank does not exist or is not owned by the current user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/transactions:
    post:
      summary: Create deposit or withdraw
      security:
        - cookieAuth: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionCreateRequest'
      responses:
        '200':
          description: Existing idempotent transaction result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionResponse'
        '201':
          description: Transaction created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/bets:
    get:
      summary: List bets
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: bankId
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Bet list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BetListResponse'
    post:
      summary: Create bet ticket
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BetCreateRequest'
      responses:
        '201':
          description: Bet created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BetResponse'

  /api/bets/{betId}:
    get:
      summary: Get bet details
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BetId'
      responses:
        '200':
          description: Bet details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BetResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/bets/{betId}/settle:
    post:
      summary: Settle bet
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BetId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BetSettleRequest'
      responses:
        '200':
          description: Bet settled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BetResponse'

  /api/bets/{betId}/cashout:
    post:
      summary: Cashout partial
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BetId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BetCashoutRequest'
      responses:
        '200':
          description: Cashout applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BetCashoutResponse'

  /api/catalog/teams:
    get:
      summary: Search teams
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          schema:
            type: string
      responses:
        '200':
          description: Team results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogListResponse'

  /api/catalog/competitions:
    get:
      summary: Search competitions
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          schema:
            type: string
      responses:
        '200':
          description: Competition results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogListResponse'

  /api/catalog/manual:
    post:
      summary: Create manual catalog entry
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CatalogManualRequest'
      responses:
        '201':
          description: Catalog item created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogItemResponse'

  /api/goals:
    get:
      summary: List goals
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Goal list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalListResponse'
    post:
      summary: Create goal
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GoalCreateRequest'
      responses:
        '201':
          description: Goal created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalResponse'

  /api/goals/{goalId}:
    get:
      summary: Get goal details
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/GoalId'
      responses:
        '200':
          description: Goal details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalResponse'

  /api/goals/{goalId}/recalculate:
    post:
      summary: Recalculate goal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/GoalId'
      responses:
        '200':
          description: Goal recalculated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalResponse'

  /api/goals/{goalId}/close:
    post:
      summary: Close goal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/GoalId'
      responses:
        '200':
          description: Goal closed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /api/recommendations:
    get:
      summary: List recommendations
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: type
          schema:
            type: string
            enum: [pre, live]
      responses:
        '200':
          description: Recommendation list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecommendationListResponse'
    post:
      summary: Create recommendation (admin)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecommendationCreateRequest'
      responses:
        '201':
          description: Recommendation created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecommendationResponse'

  /api/recommendations/{recommendationId}/follow:
    post:
      summary: Follow recommendation
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RecommendationId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecommendationFollowRequest'
      responses:
        '200':
          description: Prefilled bet payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrefillBetResponse'

  /api/metrics/overview:
    get:
      summary: Get metrics overview
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: bankId
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Metrics summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetricsResponse'

components:
  securitySchemes:
    bearerAuth:
      type: apiKey
      in: cookie
      name: sb-<project-ref>-auth-token
      description: Legacy scheme name. This web API uses the Supabase BFF session cookie, never an Authorization bearer token.
    cookieAuth:
      type: apiKey
      in: cookie
      name: sb-<project-ref>-auth-token

  parameters:
    BankId:
      in: path
      name: bankId
      required: true
      schema:
        type: string
        format: uuid
    BetId:
      in: path
      name: betId
      required: true
      schema:
        type: string
        format: uuid
    GoalId:
      in: path
      name: goalId
      required: true
      schema:
        type: string
        format: uuid
    RecommendationId:
      in: path
      name: recommendationId
      required: true
      schema:
        type: string
        format: uuid
    IdempotencyKey:
      in: header
      name: Idempotency-Key
      required: true
      schema:
        type: string
        format: uuid

  responses:
    BadRequest:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Authenticated user has no access to the resource
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Idempotency key was reused with a different request payload
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ServerError:
      description: Server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
      required: [success]

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
        details:
          type: string
        field:
          type: string
      required: [error]

    RegisterRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
      required: [email, password]

    LoginRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
      required: [email, password]

    ResetPasswordRequest:
      type: object
      properties:
        email:
          type: string
          format: email
      required: [email]

    AuthResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
      required: [success, message]

    BankCreateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        currency:
          type: string
          enum: [EUR, USD, ARS]
        initialCash:
          type: number
          minimum: 0.01
          multipleOf: 0.01
        initialBonus:
          type: number
          minimum: 0.01
          multipleOf: 0.01
        initialFreebet:
          type: number
          minimum: 0.01
          multipleOf: 0.01
      required: [name, currency, initialCash, initialBonus, initialFreebet]

    BankResponse:
      type: object
      properties:
        success:
          type: boolean
        bank:
          $ref: '#/components/schemas/Bank'
      required: [success, bank]

    BankListResponse:
      type: object
      properties:
        success:
          type: boolean
        banks:
          type: array
          items:
            $ref: '#/components/schemas/Bank'
      required: [success, banks]

    Bank:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        currency:
          type: string
        balances:
          type: object
          properties:
            cash:
              type: number
            bonus:
              type: number
            freebet:
              type: number
            operative:
              type: number
          required: [cash, bonus, freebet, operative]
      required: [id, name, currency, balances]

    TransactionCreateRequest:
      type: object
      properties:
        bankId:
          type: string
          format: uuid
        type:
          type: string
          enum: [deposit, withdraw]
        amount:
          type: number
          exclusiveMinimum: 0
          multipleOf: 0.01
        method:
          type: string
          enum: [bank_transfer, card, cash]
      required: [bankId, type, amount, method]

    TransactionResponse:
      type: object
      properties:
        success:
          type: boolean
        transactionId:
          type: string
          format: uuid
        balance:
          type: number
      required: [success, transactionId, balance]

    TransferCreateRequest:
      type: object
      properties:
        toBankId:
          type: string
          format: uuid
        amount:
          type: number
          exclusiveMinimum: 0
          multipleOf: 0.01
      required: [toBankId, amount]

    TransferResponse:
      type: object
      properties:
        success:
          type: boolean
        transferId:
          type: string
          format: uuid
        sourceBalance:
          type: number
        destinationBalance:
          type: number
      required: [success, transferId, sourceBalance, destinationBalance]

    BetCreateRequest:
      type: object
      properties:
        bankId:
          type: string
          format: uuid
        legs:
          type: array
          items:
            $ref: '#/components/schemas/BetLegInput'
        odds:
          type: number
        stakeAmount:
          type: number
        stakeLevel:
          type: number
        funding:
          type: object
          properties:
            cash:
              type: number
            bonus:
              type: number
            freebet:
              type: number
      required: [bankId, legs, odds]

    BetLegInput:
      type: object
      properties:
        eventId:
          type: string
          format: uuid
        market:
          type: string
        selection:
          type: string
        odds:
          type: number
      required: [market, selection, odds]

    BetResponse:
      type: object
      properties:
        success:
          type: boolean
        bet:
          $ref: '#/components/schemas/Bet'
      required: [success, bet]

    BetListResponse:
      type: object
      properties:
        success:
          type: boolean
        bets:
          type: array
          items:
            $ref: '#/components/schemas/Bet'

    Bet:
      type: object
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
        stakeAmount:
          type: number
        stakeLevel:
          type: number
        odds:
          type: number
        legs:
          type: array
          items:
            $ref: '#/components/schemas/BetLeg'

    BetLeg:
      type: object
      properties:
        market:
          type: string
        selection:
          type: string
        odds:
          type: number

    BetSettleRequest:
      type: object
      properties:
        result:
          type: string
          enum: [win, lose, void, half_win, half_loss]
        settlementAmount:
          type: number
      required: [result]

    BetCashoutRequest:
      type: object
      properties:
        cashoutAmount:
          type: number
        remainingStake:
          type: number
      required: [cashoutAmount, remainingStake]

    BetCashoutResponse:
      type: object
      properties:
        success:
          type: boolean
        closedBetId:
          type: string
          format: uuid
        openBetId:
          type: string
          format: uuid

    CatalogManualRequest:
      type: object
      properties:
        type:
          type: string
          enum: [team, competition]
        rawText:
          type: string
        country:
          type: string
      required: [type, rawText]

    CatalogItemResponse:
      type: object
      properties:
        success:
          type: boolean
        itemId:
          type: string
          format: uuid

    CatalogListResponse:
      type: object
      properties:
        success:
          type: boolean
        items:
          type: array
          items:
            type: object

    GoalCreateRequest:
      type: object
      properties:
        bankId:
          type: string
          format: uuid
        baseAmount:
          type: number
        targetAmount:
          type: number
        deadline:
          type: string
          format: date
        stakePreference:
          type: number
        strategy:
          type: string
          enum: [conservative, accelerated]
      required: [bankId, baseAmount, targetAmount, deadline]

    GoalResponse:
      type: object
      properties:
        success:
          type: boolean
        goal:
          $ref: '#/components/schemas/Goal'

    GoalListResponse:
      type: object
      properties:
        success:
          type: boolean
        goals:
          type: array
          items:
            $ref: '#/components/schemas/Goal'

    Goal:
      type: object
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
        baseAmount:
          type: number
        targetAmount:
          type: number
        deadline:
          type: string
          format: date
        dailyProfit:
          type: number
        suggestedOdds:
          type: number

    RecommendationCreateRequest:
      type: object
      properties:
        eventId:
          type: string
          format: uuid
        market:
          type: string
        odds:
          type: number
        type:
          type: string
          enum: [pre, live]
        icp:
          type: integer
      required: [eventId, market, odds, type, icp]

    RecommendationFollowRequest:
      type: object
      properties:
        bankId:
          type: string
          format: uuid
      required: [bankId]

    RecommendationResponse:
      type: object
      properties:
        success:
          type: boolean
        recommendation:
          $ref: '#/components/schemas/Recommendation'

    RecommendationListResponse:
      type: object
      properties:
        success:
          type: boolean
        recommendations:
          type: array
          items:
            $ref: '#/components/schemas/Recommendation'

    Recommendation:
      type: object
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
        market:
          type: string
        odds:
          type: number
        icp:
          type: integer

    PrefillBetResponse:
      type: object
      properties:
        success:
          type: boolean
        prefill:
          type: object
          properties:
            eventId:
              type: string
              format: uuid
            market:
              type: string
            odds:
              type: number

    MetricsResponse:
      type: object
      properties:
        success:
          type: boolean
        metrics:
          type: object
          properties:
            yieldCash:
              type: number
            yieldOperative:
              type: number
            winRate:
              type: number
